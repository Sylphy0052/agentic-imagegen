# anyloraCheckpoint_bakedvaeBlessedFp16.safetensors 向けの画風preset。
# 画風と品質タグ、それに合うサンプラー設定を書く。
# 解像度とseedは再現性に直結するため、styleでは指定せずSpec側に任せる。
# 照明はシーンの一部としてscene側で指定する。
#
# 値の出どころは、同じcheckpoint (sha256先頭10桁 ef49fbb25f) をA1111 v1.10.1で
# 運用したときの実績設定 (生成画像のPNG infoから復元)。
#
# このcheckpointはLoRAを載せる土台としてニュートラルに作られている。
# style側で画風を寄せるとLoRAの画風と競合するため、品質タグだけに絞っている。
# 単体で使うより model.loras と組み合わせる方が本来の用途。
#
# sampler に dpmpp_2m_sde を選んでいるため、steps を 20 より下げない。
# SDE系は低stepsで収束せず絵が破綻する。低負荷で流したいときは
# Spec側で sampler: euler_ancestral などへ上書きする。
#
# clip_skip と外部VAEもこのpresetが持つ。A1111での実績設定のうち、Spec側に書く必要が
# 残るのは次の2つ (書き方は docs/prompting-guide.md の「A1111から設定を移す」を参照)。
#   model.checkpoint: anyloraCheckpoint_bakedvaeBlessedFp16.safetensors
#   generation.upscale: RealESRGAN_x4plus_anime_6B.pth / scale 2.0 / denoise 0.65 / steps 20

description: AnyLoRA向け。LoRAの画風を邪魔しない品質タグだけの構成

# サンプラー設定と品質タグはこのcheckpointに合わせて決めた値。別のcheckpointへ
# 流用すると噛み合わないため、対象を明示して validate で取り違えを警告させる。
applies_to:
  - anyloraCheckpoint_bakedvaeBlessedFp16.safetensors

prompt:
  positive: >
    masterpiece, best quality, ultra-detailed, highres

  negative: >
    embedding:negativeXL_D, nsfw, super deformed, bad anatomy, bad hands,
    extra fingers, worst quality, low quality, normal quality, lowres, blurry,
    jpeg artifacts, watermark, text, logo, signature, speech bubble, 3d,
    photorealistic, harsh lighting, lens flare, film grain, realistic,
    monochrome, censored, multiple views

generation:
  sampler: dpmpp_2m_sde
  scheduler: exponential
  cfg: 7.0
  steps: 30

model:
  clip_skip: 2
  vae: vaeKlF8Anime2_klF8Anime2VAE.safetensors
