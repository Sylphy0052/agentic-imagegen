# Anima系 (DiT + Qwen3-0.6B) 向けの画風preset。
# 配布元が推奨する品質タグとサンプラー設定をまとめる。
# 解像度とseedは再現性に直結するため、styleでは指定せずSpec側に任せる。
# Animaは1024x1024前後が前提のため、Spec側で832x1216などを指定する。
# model ブロックは持たない。DiT系はtext encoderの構造が違うためclip_skipと併用できず、
# VAEは model.unet / clip とセットでSpec側へ書く必須項目のため。

description: Anima系の既定設定。danbooruタグと自然文のどちらでも使える

prompt:
  positive: >
    masterpiece, best quality, score_7, safe

  negative: >
    worst quality, low quality, score_1, score_2, score_3, artist name, blurry,
    jpeg artifacts, chromatic aberration

generation:
  sampler: er_sde
  scheduler: simple
  cfg: 4.5
  steps: 32
